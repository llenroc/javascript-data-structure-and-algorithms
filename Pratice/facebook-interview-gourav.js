console.log("Facebook interview questions");

/*

Q1. Implement emitter, to emit and sub.release method.

const emitter = new Emitter();

const sub = emitter.subscribe(eventName, callback);

emitter.emit("eventName", value1, value2);

sub.release();

*/















/*

Q2.  Implement clearAllTimeouts to clear all the timeout in the app.

window.setTimeout(fn1, wait);
window.setTimeout(fn2, wait);
window.setTimeout(fn3, wait);
window.setTimeout(fn4, wait);

function clearAllTimeouts() {

}

*/